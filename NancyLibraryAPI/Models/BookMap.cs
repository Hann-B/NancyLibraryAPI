using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using FluentNHibernate.Mapping;

namespace NancyLibraryAPI.Models
{
    public class BookMap
    {
        public BookMap()
        {
            //Table("Book");

            //Id(x => x.Id, "Id").GeneratedBy.Identity().UnsavedValue(0);

            //Map(x => x.Title);
            //Map(x => x.Author);
            //Map(x => x.YearPublished);
            //Map(x => x.Genre);
            //Map(x => x.IsCheckedOut);
            //Map(x => x.LastCheckedOutDate);
            //Map(x => x.DueBackDate);
            //Map(x => x.NeatLastCheckedOutDate);
            //Map(x => x.NeatDueBackDate);

        }

    }
}